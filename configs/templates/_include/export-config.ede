    -- * Configuration
    {% if api.service.alias %}
    {{ api.service.alias.name }},
    {% endif %}
    {{ api.service.ctor.name }},

    -- ** Global Request Parameters
    {{ api.service.name }} (..),
  {% for scope in api.scopes %}
    {% if scope.first %}

    -- ** OAuth Scopes
    {% endif %}
    {{ scope.value.name }},
  {% endfor %}