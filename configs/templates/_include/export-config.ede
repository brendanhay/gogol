    -- * Configuration
    {{ api.config.name }},

    -- ** Global Request Parameters
    {{ api.params.name }} (..),
    {{ api.params.ctor.name }},
  {% for scope in api.scopes %}
    {% if scope.first %}

    -- ** OAuth Scopes
    {% endif %}
    {{ scope.value.name }},
  {% endfor %}